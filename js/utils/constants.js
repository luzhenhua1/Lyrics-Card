/**
 * 应用常量定义
 */

// 默认调色板
export const FALLBACK_PALETTE = [
  [40, 90, 180],
  [80, 140, 220],
  [20, 60, 120],
  [100, 160, 200],
];

// 默认封面
export const DEFAULT_COVER = 'fengmian.webp';

// 应用版本
export const APP_VERSION = '1.0';

// 图片压缩限制 (200KB)
export const IMAGE_COMPRESSION_LIMIT = 200 * 1024;